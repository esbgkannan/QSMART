mydata = read.table('../../TrainingSet/FullSet/AvgRank/central_nervous_system.csv',head=T,sep=",")
model = lm(IC50 ~ factor(Cancer) + factor(Drug), data=mydata)
sse <- c(crossprod(model$residuals))
sink('./central_nervous_system.txt',append=TRUE)
print(summary(model))
print(sse)
sink()
