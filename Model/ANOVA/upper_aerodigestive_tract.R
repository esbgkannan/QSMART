mydata = read.table('../../TrainingSet/FullSet/AvgRank/upper_aerodigestive_tract.csv',head=T,sep=",")
model = lm(IC50 ~ factor(Cancer) + factor(Drug), data=mydata)
sse <- c(crossprod(model$residuals))
sink('./upper_aerodigestive_tract.txt',append=TRUE)
print(summary(model))
print(sse)
sink()
